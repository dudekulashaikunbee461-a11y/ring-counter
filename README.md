# ring-counter
A **Ring Counter** is a sequential circuit made of flip-flops where the output of the last flip-flop is fed back to the first. It circulates a single `1` (or `0`) through the flip-flops with each clock pulse, producing a repeating sequence.
